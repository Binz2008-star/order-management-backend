# Neon branch workflow temporarily disabled to unblock CI
# TODO: Re-enable after Neon API issues resolved
name: Neon Branch (Disabled)

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "Neon branch workflow temporarily disabled - using main CI pipeline"
